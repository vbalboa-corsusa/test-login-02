@{
    ViewData["Title"] = "Home Page";
}

<html>
<head>
    <title>Login</title>
    <link href="//maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" rel="stylesheet" id="bootstrap-css">
    <script src="//maxcdn.bootstrapcdn.com/bootstrap/4.0.0/js/bootstrap.min.js"></script>
    <script src="//cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
	<link href="~/css/site.css" rel="stylesheet" />
</head>
<body>
    <div class="wrapper fadeInDown">
        <h2>Inicio de sesión Bitrix</h2>
        <div id="formContent">
            <!-- Tabs Titles -->

            <!-- Login Form -->
            <a href="@Url.Action("Authorize", "OAuth")" class="btn btn-primary">Ingresar con Bitrix</a>


            @* <form method="post" action="@Url.Action("Login","Home")"> *@
            @*     <label for="EMAIL">Ingrese su correo:</label> *@
            @*     <input type="email" id="email" class="form-control" name="email" placeholder="Ejemplo: pvercel@corsusa.com" required> *@
            @*     <label for="password">Ingrese su contraseña:</label> *@
            @*     <input type="password" id="password" class="form-control" name="password" required> *@
            @*     <input type="submit" class="form-control" value="Log In"> *@
            @*     <button type="submit" class="btn btn-primary">Ingresar</button> *@
            @* </form> *@

            @if (ViewBag.Message != null)
            {
                <div class="alert alert-success mt-3">
                    @ViewBag.Message
                </div>
            }

        </div>
    </div>
</body>

</html>
